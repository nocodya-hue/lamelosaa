# La Melosa · web

Web estática (HTML, CSS y JS) con escena 3D (Three.js + GSAP).

- Abre `index.html` en el navegador, o publícala con GitHub Pages (rama `main`, carpeta `/ (root)`).
- Necesita internet para las fuentes y las librerías desde CDN.
